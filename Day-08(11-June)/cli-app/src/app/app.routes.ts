import { Routes } from '@angular/router';
import { ConfigDashboardComponent } from './components/config-dashboard/config-dashboard';

export const routes: Routes = [
  { path: '', component: ConfigDashboardComponent },
];

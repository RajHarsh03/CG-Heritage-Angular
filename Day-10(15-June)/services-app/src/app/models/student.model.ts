export interface Student {
  id: number;
  name: string;
  email: string;
  course: string;
  grade: string;
}

export interface Product {
  id: number;
  name: string;
  price: number;
  category: string;
}
